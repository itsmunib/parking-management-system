<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Badi Parking Management System</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f4f6f9;
      margin: 0;
      padding: 0;
    }
    /* Sidebar Styles */
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 250px;
      background-color: #1a252f;
      color: #ffffff;
      padding-top: 20px;
      transition: all 0.3s;
    }
    .sidebar .navbar-brand {
      font-size: 1.5rem;
      font-weight: 700;
      padding: 20px 30px;
      display: block;
      color: #ffffff;
      text-align: center;
      border-bottom: 1px solid #2c3b4e;
    }
    .sidebar .nav-link {
      color: #a4b0be;
      padding: 15px 30px;
      font-size: 1.1rem;
      display: flex;
      align-items: center;
      transition: all 0.3s;
    }
    .sidebar .nav-link i {
      margin-right: 10px;
    }
    .sidebar .nav-link:hover, .sidebar .nav-link.active {
      color: #ffffff;
      background-color: #2c3b4e;
    }
    /* Main Content Styles */
    .main-content {
      margin-left: 250px;
      padding: 30px;
      min-height: 100vh;
      background-color: #f4f6f9;
    }
    .page-header {
      margin-bottom: 30px;
    }
    .page-header h2 {
      font-weight: 700;
      color: #2c3b4e;
    }
    /* Card Styles */
    .metric-card {
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-bottom: 20px;
      transition: transform 0.3s;
    }
    .metric-card:hover {
      transform: translateY(-5px);
    }
    .metric-card .card-title {
      font-size: 1.2rem;
      font-weight: 500;
      color: #666;
      margin-bottom: 10px;
    }
    .metric-card .card-value {
      font-size: 2rem;
      font-weight: 700;
      color: #2c3b4e;
    }
    .metric-card .card-icon {
      font-size: 2.5rem;
      color: #2c3b4e;
      opacity: 0.1;
      position: absolute;
      right: 20px;
      top: 20px;
    }
    /* Space Usage Styles */
    .space-usage .progress {
      height: 20px;
      border-radius: 10px;
      margin-bottom: 10px;
    }
    .space-usage .progress-bar.available {
      background-color: #28a745;
    }
    .space-usage .progress-bar.parked {
      background-color: #dc3545;
    }
    .space-usage .progress-label {
      font-size: 0.9rem;
      color: #666;
    }
    /* Filter Buttons */
    .filter-buttons .btn {
      margin: 0 5px;
      border-radius: 20px;
      padding: 8px 20px;
      font-weight: 500;
      transition: all 0.3s;
    }
    .filter-buttons .btn-primary {
      background-color: #2c3b4e;
      border-color: #2c3b4e;
    }
    .filter-buttons .btn-primary:hover {
      background-color: #3b4a5a;
      border-color: #3b4a5a;
    }
    .filter-buttons .btn-secondary {
      background-color: #e9ecef;
      color: #2c3b4e;
      border-color: #e9ecef;
    }
    .filter-buttons .btn-secondary:hover {
      background-color: #d3d7da;
      border-color: #d3d7da;
    }
    /* Responsive Design */
    @media (max-width: 768px) {
      .sidebar {
        width: 200px;
        transform: translateX(-200px);
      }
      .sidebar.active {
        transform: translateX(0);
      }
      .main-content {
        margin-left: 0;
      }
      .sidebar-toggle {
        display: block;
        position: fixed;
        top: 15px;
        left: 15px;
        font-size: 1.5rem;
        color: #2c3b4e;
        cursor: pointer;
        z-index: 1000;
      }
    }
    @media (min-width: 769px) {
      .sidebar-toggle {
        display: none;
      }
    }
  </style>
</head>
<body>
  <!-- Sidebar Toggle for Mobile -->
  <i class="fas fa-bars sidebar-toggle"></i>

  <!-- Sidebar -->
  <nav class="sidebar">
    <a class="navbar-brand" href="/dashboard">Badi Parking System</a>
    <ul class="nav flex-column">
      <% if (user) { %>
        <% if (user.permissions.entry || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/entry"><i class="fas fa-sign-in-alt"></i> Entry</a>
          </li>
        <% } %>
        <% if (user.permissions.exit || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/exit"><i class="fas fa-sign-out-alt"></i> Exit</a>
          </li>
        <% } %>
        <% if (user.permissions.manage || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/manage"><i class="fas fa-cogs"></i> Manage</a>
          </li>
        <% } %>
        <% if (user.permissions.profile || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/profile"><i class="fas fa-user"></i> Profile</a>
          </li>
        <% } %>
        <% if (user.permissions.add_admin || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/add-admin"><i class="fas fa-user-plus"></i> Add Admin</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manage-admins"><i class="fas fa-users-cog"></i> Manage Admins</a>
          </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </li>
      <% } %>
    </ul>
  </nav>

  <!-- Main Content -->
  <div class="main-content">
    <div class="page-header">
      <h2>Dashboard</h2>
    </div>

    <% if (error) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <div class="row">
      <div class="col-md-4">
        <div class="metric-card position-relative">
          <i class="fas fa-car card-icon"></i>
          <h5 class="card-title">Vehicles (<%= filter.charAt(0).toUpperCase() + filter.slice(1) %>)</h5>
          <p class="card-value"><%= vehicles %></p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="metric-card position-relative">
          <i class="fas fa-money-bill-wave card-icon"></i>
          <h5 class="card-title">Earnings (<%= filter.charAt(0).toUpperCase() + filter.slice(1) %>)</h5>
          <p class="card-value">PKR <%= (Number(earnings) || 0).toFixed(2) %></p>
        </div>
      </div>
      <div class="col-md-4">
        <div class="metric-card position-relative">
          <i class="fas fa-parking card-icon"></i>
          <h5 class="card-title">Space Usage</h5>
          <div class="space-usage">
            <div class="progress-label">Available Spaces: <%= available %></div>
            <div class="progress">
              <div class="progress-bar available" role="progressbar" style="width: <%= totalSpaces > 0 ? (available / totalSpaces) * 100 : 0 %>%;" aria-valuenow="<%= totalSpaces > 0 ? (available / totalSpaces) * 100 : 0 %>" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
            <div class="progress-label">Used Spaces: <%= totalSpaces - available %></div>
            <div class="progress">
              <div class="progress-bar parked" role="progressbar" style="width: <%= totalSpaces > 0 ? ((totalSpaces - available) / totalSpaces) * 100 : 0 %>%;" aria-valuenow="<%= totalSpaces > 0 ? ((totalSpaces - available) / totalSpaces) * 100 : 0 %>" aria-valuemin="0" aria-valuemax="100"></div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="filter-buttons text-center">
      <a href="/dashboard?filter=today" class="btn btn-<%= filter === 'today' ? 'primary' : 'secondary' %>">Today</a>
      <a href="/dashboard?filter=weekly" class="btn btn-<%= filter === 'weekly' ? 'primary' : 'secondary' %>">Weekly</a>
      <a href="/dashboard?filter=monthly" class="btn btn-<%= filter === 'monthly' ? 'primary' : 'secondary' %>">Monthly</a>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-4" style="position: fixed; bottom: 0; width: calc(100% - 250px); margin-left: 250px;">
    <p>© <%= new Date().getFullYear() %> Badi Parking Management System. All rights reserved.</p>
  </footer>

  <!-- Bootstrap JS and Sidebar Toggle Script -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    $(document).ready(function() {
      $('.sidebar-toggle').click(function() {
        $('.sidebar').toggleClass('active');
      });
    });
  </script>
</body>
</html>