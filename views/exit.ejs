<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vehicle Exit - Badi Parking Management System</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background-color: #f4f6f9;
      margin: 0;
      padding: 0;
    }
    /* Sidebar Styles */
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 250px;
      background-color: #1a252f;
      color: #ffffff;
      padding-top: 20px;
      transition: all 0.3s;
    }
    .sidebar .navbar-brand {
      font-size: 1.5rem;
      font-weight: 700;
      padding: 20px 30px;
      display: block;
      color: #ffffff;
      text-align: center;
      border-bottom: 1px solid #2c3b4e;
    }
    .sidebar .nav-link {
      color: #a4b0be;
      padding: 15px 30px;
      font-size: 1.1rem;
      display: flex;
      align-items: center;
      transition: all 0.3s;
    }
    .sidebar .nav-link i {
      margin-right: 10px;
    }
    .sidebar .nav-link:hover, .sidebar .nav-link.active {
      color: #ffffff;
      background-color: #2c3b4e;
    }
    /* Main Content Styles */
    .main-content {
      margin-left: 250px;
      padding: 30px;
      min-height: 100vh;
      background-color: #f4f6f9;
    }
    .page-header {
      margin-bottom: 30px;
    }
    .page-header h2 {
      font-weight: 700;
      color: #2c3b4e;
    }
    /* Table Styles */
    .entries-table-card {
      background-color: #ffffff;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-top: 20px;
    }
    .entries-table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
    }
    .entries-table thead th {
      background-color: #2c3b4e;
      color: #ffffff;
      padding: 15px;
      font-weight: 500;
      text-align: left;
      border-bottom: 2px solid #dee2e6;
    }
    .entries-table thead th:first-child {
      border-top-left-radius: 8px;
    }
    .entries-table thead th:last-child {
      border-top-right-radius: 8px;
    }
    .entries-table tbody tr {
      transition: background-color 0.3s;
    }
    .entries-table tbody tr:hover {
      background-color: #f8f9fa;
    }
    .entries-table tbody td {
      padding: 15px;
      border-bottom: 1px solid #dee2e6;
      color: #2c3b4e;
    }
    .entries-table .btn-danger {
      background-color: #dc3545;
      border-color: #dc3545;
      padding: 8px 16px;
      font-weight: 500;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .entries-table .btn-danger:hover {
      background-color: #c82333;
      border-color: #bd2130;
    }
    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 20px;
      color: #666;
      font-style: italic;
    }
    /* Responsive Design */
    @media (max-width: 768px) {
      .sidebar {
        width: 200px;
        transform: translateX(-200px);
      }
      .sidebar.active {
        transform: translateX(0);
      }
      .main-content {
        margin-left: 0;
      }
      .sidebar-toggle {
        display: block;
        position: fixed;
        top: 15px;
        left: 15px;
        font-size: 1.5rem;
        color: #2c3b4e;
        cursor: pointer;
        z-index: 1000;
      }
      .entries-table-card {
        padding: 10px;
      }
      .entries-table thead th, .entries-table tbody td {
        padding: 10px;
        font-size: 0.9rem;
      }
      .entries-table .btn-danger {
        padding: 6px 12px;
        font-size: 0.9rem;
      }
    }
    @media (min-width: 769px) {
      .sidebar-toggle {
        display: none;
      }
    }
  </style>
</head>
<body>
  <!-- Sidebar Toggle for Mobile -->
  <i class="fas fa-bars sidebar-toggle"></i>

  <!-- Sidebar -->
  <nav class="sidebar">
    <a class="navbar-brand" href="/dashboard">Badi Parking System</a>
    <ul class="nav flex-column">
      <% if (user) { %>
        <% if (user.permissions.entry || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/entry"><i class="fas fa-sign-in-alt"></i> Entry</a>
          </li>
        <% } %>
        <% if (user.permissions.exit || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link active" href="/exit"><i class="fas fa-sign-out-alt"></i> Exit</a>
          </li>
        <% } %>
        <% if (user.permissions.manage || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/manage"><i class="fas fa-cogs"></i> Manage</a>
          </li>
        <% } %>
        <% if (user.permissions.profile || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/profile"><i class="fas fa-user"></i> Profile</a>
          </li>
        <% } %>
        <% if (user.permissions.add_admin || user.username === 'superadmin') { %>
          <li class="nav-item">
            <a class="nav-link" href="/add-admin"><i class="fas fa-user-plus"></i> Add Admin</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/manage-admins"><i class="fas fa-users-cog"></i> Manage Admins</a>
          </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </li>
      <% } %>
    </ul>
  </nav>

  <!-- Main Content -->
  <div class="main-content">
    <div class="page-header">
      <h2>Vehicle Exit</h2>
    </div>

    <% if (error) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <!-- Entries Table -->
    <div class="entries-table-card">
      <table class="entries-table">
        <thead>
          <tr>
            <th>Number Plate</th>
            <th>Entry Time</th>
            <th>Owner Name</th>
            <th>Phone</th>
            <th>Category</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          <% if (entries && entries.length > 0) { %>
            <% entries.forEach(entry => { %>
              <tr>
                <td><%= entry.number_plate %></td>
                <td><%= new Date(entry.entry_time).toLocaleString('en-US', { timeZone: 'Asia/Karachi' }) %></td>
                <td><%= entry.owner_name || 'N/A' %></td>
                <td><%= entry.phone || 'N/A' %></td>
                <td><%= entry.category || 'Unknown' %></td>
                <td>
                  <form action="/exit" method="POST">
                    <input type="hidden" name="entry_id" value="<%= entry.id %>">
                    <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to process this exit?');">Exit</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="6" class="empty-state">No vehicles currently parked.</td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-white text-center py-3 mt-4" style="position: fixed; bottom: 0; width: calc(100% - 250px); margin-left: 250px;">
    <p>© <%= new Date().getFullYear() %> Badi Parking Management System. All rights reserved.</p>
  </footer>

  <!-- Bootstrap JS and Sidebar Toggle Script -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    $(document).ready(function() {
      $('.sidebar-toggle').click(function() {
        $('.sidebar').toggleClass('active');
      });
    });
  </script>
</body>
</html>